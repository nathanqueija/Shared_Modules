import React from 'react'

export default ({ className }) => (
  <div className={className}>
    <p>We could not find any results.</p>
    <p>Please, try a new search with different terms.</p>
  </div>
)
